# repopy

A modular, cross-platform Python CLI tool and library designed to automate the scaffolding and initialization of new Python workspaces.

## Features
- **Local Scaffolding**: Automatically creates directory structures, seeds template files, and spins up local virtual environments.

- **Repository Cloning**: Clones remote codebases, provisions isolated environments, and handles automated dependency updates via requirements files.
